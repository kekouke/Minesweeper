<Window x:Class="Minesweeper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper"
        mc:Ignorable="d"
        Title="Minesweeper" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.05*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.4*"/>
        </Grid.ColumnDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Restart" Command="{Binding RestartGameCommand}"></MenuItem>
        </Menu>
        
        <Frame Grid.Column="0" Grid.Row="1" Background="Aqua"></Frame>
        <Canvas Grid.Column="1" Grid.Row="1" x:Name="Canvas" MouseLeftButtonDown="Canvas_MouseLeftButtonDown">
        </Canvas>
        <Frame Grid.Column="2" Grid.Row="1" Background="Aqua"></Frame>
    </Grid>
</Window>
