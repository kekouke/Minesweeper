<Window x:Class="Minesweeper.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper.Views"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Minesweeper" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.4*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.4*"/>
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="0.05*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Menu Grid.Row="0">
                <MenuItem Header="Levels" />
                <MenuItem Header="Restart" Command="{Binding RestartGameCommand}"/>
                <MenuItem Header="About" />
            </Menu>
            <local:GameBoardView Grid.Row="1"
                                 Grid.Column="1">
                
            </local:GameBoardView>
            <Frame Grid.Column="0" Grid.Row="1" Background="Aqua"></Frame>
            <Frame Grid.Column="2" Grid.Row="1" Background="Aqua"/>
        </Grid>
    </Grid>
</Window>
