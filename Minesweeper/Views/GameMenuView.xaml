<UserControl x:Class="Minesweeper.Views.GameMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Minesweeper.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Menu VerticalAlignment="Stretch">
            <MenuItem Header="Levels">
                <MenuItem Header="Beginner" Command="{Binding BeginnerNewGameCommand}"/>
                <MenuItem Header="Advanced" Command="{Binding AdvancedNewGameCommand}"/>
                <MenuItem Header="Expert" Command="{Binding ExpertNewGameCommand}"/> 
            </MenuItem>
            <MenuItem Header="Restart" Command="{Binding RestartGameCommand}"/>
            <MenuItem Header="Leaderboad"/>
            <MenuItem Header="About" />
        </Menu>
    </Grid>
</UserControl>
